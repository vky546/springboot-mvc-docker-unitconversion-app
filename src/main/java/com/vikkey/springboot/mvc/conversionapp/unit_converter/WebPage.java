package com.vikkey.springboot.mvc.conversionapp.unit_converter;

/* 
 * Enum will be used to set the page request, 
 * so that we can decide what units, colors etc
 * need to set.
 */
public enum WebPage {
	TEMP, 
	LENGTH, 
	TIME, 
	AREA;
}
